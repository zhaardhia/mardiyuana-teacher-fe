export type AcademicYearEnrolled = {
  id: string
  academicYear: string
  status: string
  curriculumId: string
  curriculumName: string
  createdDate?: Date
  updatedDate?: Date
}

export type AcademicYearInitialCourse = {
  id: string
  academicYear: string
  status: string
}
